<local:OverlayWindow x:Class="TouchpadGestures_Advanced.ForBrowser"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TouchpadGestures_Advanced"
        mc:Ignorable="d"
        Background="#c5000000"
        WindowState="Normal"
        ShowInTaskbar="False"
        Left="30"
        Top="30"
        SizeToContent="WidthAndHeight"
        Title="ForBrowser">
    <local:OverlayWindow.Resources>
        <Style TargetType="local:TabWithImage2">
            <Setter Property="Margin" Value="0" />
        </Style>
        <Style TargetType="TextBlock" x:Key="MyLabel">
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
    </local:OverlayWindow.Resources>
    <Grid>
        <Grid Margin="20">
            <StackPanel Orientation="Horizontal" x:Name="WrapperSP">
            </StackPanel>
        </Grid>
    </Grid>
</local:OverlayWindow>
